server:
  port: 8884

spring:
  application:
    name: spring-cloud-clientb
  cloud:
    config:
      profile: dev

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8880/eureka/